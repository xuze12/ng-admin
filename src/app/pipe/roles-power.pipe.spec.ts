import { RolesPowerPipe } from './roles-power.pipe';

describe('RolesPowerPipe', () => {
  it('create an instance', () => {
    const pipe = new RolesPowerPipe();
    expect(pipe).toBeTruthy();
  });
});
