import { OrganizaTypePipe } from './organiza-type.pipe';

describe('OrganizaTypePipe', () => {
  it('create an instance', () => {
    const pipe = new OrganizaTypePipe();
    expect(pipe).toBeTruthy();
  });
});
